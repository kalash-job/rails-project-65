# == Schema Information
#
# Table name: categories
#
#  id         :integer          not null, primary key
#  name       :string
#  created_at :datetime         not null
#  updated_at :datetime         not null
#

one:
  name: <%= Faker::Lorem.paragraph_by_chars(number:30) %>

two:
  name: <%= Faker::Lorem.paragraph_by_chars(number:30) %>
